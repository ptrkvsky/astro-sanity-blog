import type { TableOfContent } from '@interfaces/TableOfContent';

/**
 *
 * @param headings - Array of headings from markdown file
 * @returns HTML ordered list of headings
 */
const getTableOfContents = (headings: TableOfContent[]) => {};
